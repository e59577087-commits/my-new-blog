---
import type { Tool } from "../data/tools";

type Props = {
  tool: Tool;
};

const { tool } = Astro.props;
---

<article class="surface-soft motion-card rounded-lg p-4 sm:p-5">
  <div class="flex items-center gap-4">
    <img src={tool.screenshot} alt="" loading="lazy" class="size-16 shrink-0 rounded-md border hairline object-cover sm:size-20" />
    <div class="min-w-0">
      <p class="text-xs text-sky-300/80">{tool.category}</p>
      <h2 class="mt-1 truncate text-xl font-semibold leading-7 text-white">{tool.name}</h2>
      <div class="mt-2 flex flex-wrap gap-1.5">
        {tool.tags.map((tag) => <span class="rounded bg-[#0f1115] px-2 py-0.5 text-xs text-slate-400">{tag}</span>)}
      </div>
    </div>
  </div>
</article>
